to run server:
    - python3 manage.py runserver; in /server/backendLicenta (django api)
    - yarn dev; in /client/app (react)

to get a list of active hosts and respective vendor names on local network:
    sudo arp-scan --plain --format='${ip}, ${vendor}' -l

